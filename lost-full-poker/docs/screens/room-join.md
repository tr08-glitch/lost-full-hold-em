# ルーム参加画面 仕様

『混沌』のルーム参加画面構成をベースにしつつ、ルームコードの方式に合わせて変更。

## ルームコードについて
ルームコードは、サーバー(Colyseus)が各ルーム作成時に自動発行する英数字のID(roomId)をそのまま使用する。
4桁数字ではなくテンキー入力の対象にならないため、テキスト入力欄に変更した。

## レイアウト
- ルームコード入力欄(英数字のテキスト入力)
- 入力エラー時のメッセージ表示欄(コードが存在しない・既にゲーム開始済みの場合など)
- 「参加する」ボタン(Enterキーでも実行可能)
- 閉じる用の「戻る」ボタン
